package Modelo;

/*import javax.persistence.*;
import java.util.Objects;

@Entity
@Table(name = "precioproductovendido", schema = "farmaciafei", catalog = "")
public class PrecioproductovendidoEntity {
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    @Id
    @Column(name = "idPrecioProductoVendido")
    private int idPrecioProductoVendido;
    @Basic
    @Column(name = "nombre")
    private String nombre;
    @Basic
    @Column(name = "precio")
    private Double precio;

    public int getIdPrecioProductoVendido() {
        return idPrecioProductoVendido;
    }

    public void setIdPrecioProductoVendido(int idPrecioProductoVendido) {
        this.idPrecioProductoVendido = idPrecioProductoVendido;
    }

    public String getNombre() {
        return nombre;
    }

    public void setNombre(String nombre) {
        this.nombre = nombre;
    }

    public Double getPrecio() {
        return precio;
    }

    public void setPrecio(Double precio) {
        this.precio = precio;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;
        PrecioproductovendidoEntity that = (PrecioproductovendidoEntity) o;
        return idPrecioProductoVendido == that.idPrecioProductoVendido && Objects.equals(nombre, that.nombre) && Objects.equals(precio, that.precio);
    }

    @Override
    public int hashCode() {
        return Objects.hash(idPrecioProductoVendido, nombre, precio);
    }
}*/
